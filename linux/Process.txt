모든 로그는 var폴더에 잇음.

1. migration(다른 셸을 띄움)
-> persistent 유지.
(접속 정보 확인할 수 있는 로그 수집)

2. 백도어 생성(또 들어와야함)
-> 생성할 권한이 없는 계정(서비스
재가동할 권한 루트)의 경우 권한 상승 시도를 먼저함.
-> 권한있으면 생성후 권한상승 시도.
-> 대부분 setuid 권한이 있음. find / -perm -4000

3. ㅁㅁㅁㅁㅁㅁ
history, .bash_history, .sh_history
사용자 계정별 히스토리 확보(셸 별로)

네트워크 정보 획득
(실행시점 이후 3회 이상)
-> netstat -naop -ntlp

process 정보도 마찬가지
3번이상 뜸

4. 행위 지우기(에디터를 키거나 삭제를 하거나 touch로 파일 시간을 수정하거나)
-> 메모리에 안남거나 히스토리가 없거나 프로세스 목록에 없거나 해도 swp으로 판단할 수 있다.(시점도 실데이터도)

시간을 너무 믿지마라 touch -r을 통해 수정할 수 있음. 하지만 stat 명령어를 통해 실제 변경된 시간 파악 가능.
